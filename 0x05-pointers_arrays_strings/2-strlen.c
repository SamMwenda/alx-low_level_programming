#include "main.h"
#include <stdio.h>
/**
 * _strlen - returns the length of string
 * @s: string to return length
 * Return: length of @s
 */
int _strlen(char *s)
{
	int length = sizeof(*s);
	printf("%d",length);
	return (0);
}
